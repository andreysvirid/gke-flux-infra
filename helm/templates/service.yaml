apiVersion: v1
kind: Service
metadata:
  name: {{ include "kbot.fullname" . }}
  labels:
    app: {{ include "kbot.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
  selector:
    app: {{ include "kbot.fullname" . }}
